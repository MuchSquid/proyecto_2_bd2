def search(condition):
    # Simulamos el resultado de búsqueda con una lista de tuplas (doc_id, score)
    # doc_id es el identificador del documento, y score es la puntuación de similitud
    return [(1, 0.89), (3, 0.75)]